platform :ios, '12.0'

target 'Speedo Transfer App' do
  use_frameworks!

  # Specify the Alamofire version that supports your Swift version
  pod 'Alamofire', '~> 5.6.4'
  
  # Use a compatible version of AlamofireImage
  pod 'AlamofireImage', '~> 5.5'  # Adjust to a compatible version
end
